# OS
.DS_Store
Thumbs.db

# Editors
*.swp
*.swo
*~
.idea/
.vscode/

# Cargo local config (dev-only: disables sccache wrapper)
.cargo/config.toml

# Rust build artifacts
/target/
**/target/
**/*.rs.bk

# Cargo.lock IS TRACKED, deliberately. It was ignored from the root commit
# onward, listed above under "build artifacts", which it is not: it is the
# record of which dependency versions a build actually used. This workspace
# ships a binary (batchalign3, installed across the fleet from GitHub
# Releases), and Cargo's guidance is to commit the lockfile for anything that
# produces one. Without it, the same source commit resolves a different
# dependency graph on different days, so `batchalign3 version` identifies the
# source and not the binary.

# Python
__pycache__/
*.pyc
*.pyo
.mypy_cache/
.pytest_cache/
.venv/
.venv-*/
*.egg-info/
build/
dist/

# Node
node_modules/

# Logs
*.log

# Book build output
/book/build/

# Test artifacts
corpus/reference/*.actual
corpus/reference/*.diff
spec/tmp/

# Java Chatter per-directory error logs (regenerated on every run;
# not source: retained as tests/check-error-corpus/brian-bad/00errors.cex
# which is an intentional fixture, separately excluded)
corpus/reference/**/00errors.cex
corpus/reference-xml/**/00errors.cex

# Mutation testing
mutants.out/
mutants.out.old/

# Coverage
lcov*.info

# Stray symlinks
/talkbank-tools

# Serena MCP cache (per-repo language-server indexes)
.serena/

# Vendored Swift Package Manager checkouts (should never be committed)
**/.build/
**/.tmp-review-venv/

# CLAN side-output files produced during parity testing
pipeout*.xls
pipeout*.cex
pipeout*.cha
