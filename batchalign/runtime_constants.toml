# GENERATED — DO NOT EDIT.
# Source: crates/batchalign-types/src/command_spec.rs (COMMAND_SPECS)
# Regenerate: `cargo run -p xtask -- gen-runtime-toml`
# Drift gate: `cargo run -p xtask -- gen-runtime-toml --check`
#
# Read by:
#   - Rust: batchalign-types/src/command_spec.rs (compile-time const)
#   - Python: batchalign/runtime.py (read at import time)

[cmd2task]
align = "fa"
transcribe = "asr"
transcribe_s = "asr"
translate = "translate"
morphotag = "morphosyntax"
coref = "coref"
utseg = "utterance"
benchmark = "asr,eval"
opensmile = "opensmile"
compare = "morphosyntax"
avqi = "avqi"
diarize = "speaker"

[worker_caps]
max_gpu_workers = 8
max_process_workers = 8
max_thread_workers = 8

[memory]
default_base_mb = 4000
mb_per_file_mb = 25
loading_overhead = 1.5

[gpu_heavy_commands]
commands = ["align", "transcribe", "transcribe_s", "benchmark", "diarize"]

[process_commands]
# Non-free-threaded: CPU-bound commands needing process isolation.
gil = ["morphotag", "coref", "utseg", "opensmile", "compare", "avqi"]
# Free-threaded: only native-code commands need process isolation.
free_threaded = ["opensmile", "compare", "avqi"]

# Per-command base memory (MB) for process workers (non-free-threaded).
[command_base_mb.process]
align = 4000
transcribe = 1500
transcribe_s = 2500
translate = 4000
morphotag = 8000
coref = 4000
utseg = 6000
benchmark = 1500
opensmile = 500
compare = 8000
avqi = 1500
diarize = 2500

# Per-command base memory (MB) for thread workers (free-threaded, shared models).
[command_base_mb.threaded]
align = 4000
transcribe = 1500
transcribe_s = 2500
translate = 4000
morphotag = 2000
coref = 2000
utseg = 2000
benchmark = 1500
opensmile = 500
compare = 2000
avqi = 1500
diarize = 2500

[known_engine_keys]
keys = ["asr", "batch_size", "fa", "feature_set", "utr"]
