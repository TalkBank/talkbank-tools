[package]
name = "batchalign-fa-core"
version = "0.1.0"
description = "Dependency-free numeric core for Whisper forced alignment (median filter, column standardization, DTW). Leaf crate shared by the batchalign server and the whisper pilot so the two cannot drift numerically."
edition.workspace = true
license.workspace = true
repository.workspace = true
publish = false

[dependencies]
thiserror = { workspace = true }
