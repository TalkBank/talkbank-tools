// Generated by gen_rust_tests
// DO NOT EDIT MANUALLY - regenerate from talkbank-tools spec

use talkbank_model::ErrorSink;

// Generated by gen_rust_tests - test bodies only
// DO NOT EDIT MANUALLY - regenerate from talkbank-tools spec

#[ignore = "Status: not_implemented (E003)"]
/// Tests expected behavior.
#[test]
fn test_e003_auto_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let result = parser.parse_chat_file("");

    let errors = match result {
        Ok(_) => return Err(talkbank_parser_tests::test_error::TestError::Failure("Expected parse error but parsing succeeded".to_string())),
        Err(errors) => errors,
    };

    let expected_codes = vec!["E316", "E502", "E503", "E504"];
    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}

#[ignore = "Status: not_implemented (E101)"]
/// Tests expected behavior.
#[test]
fn test_e101_auto_begin_languages_eng_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let result = parser.parse_chat_file("@Begin\n@Languages:\teng\nInvalidLine\n@Comment:\tERROR: Line format invalid\n@End");

    let errors = match result {
        Ok(_) => return Err(talkbank_parser_tests::test_error::TestError::Failure("Expected parse error but parsing succeeded".to_string())),
        Err(errors) => errors,
    };

    let expected_codes = vec!["E501", "E502", "E503", "E504"];
    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e202_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let result = parser.parse_chat_file("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Child\n@ID:\teng|corpus|CHI|||||Child|||\n*CHI:\thello@ world .\n@End");

    let errors = match result {
        Ok(_) => return Err(talkbank_parser_tests::test_error::TestError::Failure("Expected parse error but parsing succeeded".to_string())),
        Err(errors) => errors,
    };

    let expected_codes = vec!["E202"];
    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e202_missing_form_type_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let result = parser.parse_chat_file("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|||||Target_Child|||\n*CHI:\thello@ .\n@End");

    let errors = match result {
        Ok(_) => return Err(talkbank_parser_tests::test_error::TestError::Failure("Expected parse error but parsing succeeded".to_string())),
        Err(errors) => errors,
    };

    let expected_codes = vec!["E202"];
    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e202_missing_form_type_utf8_begin_languages_1() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let result = parser.parse_chat_file("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|||||Target_Child|||\n*CHI:\tdog@j .\n@End");

    let errors = match result {
        Ok(_) => return Err(talkbank_parser_tests::test_error::TestError::Failure("Expected parse error but parsing succeeded".to_string())),
        Err(errors) => errors,
    };

    let expected_codes = vec!["E203"];
    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e203_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let result = parser.parse_chat_file("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Child\n@ID:\teng|corpus|CHI|||||Child|||\n*CHI:\tdog@b@c .\n@End");

    let errors = match result {
        Ok(_) => return Err(talkbank_parser_tests::test_error::TestError::Failure("Expected parse error but parsing succeeded".to_string())),
        Err(errors) => errors,
    };

    let expected_codes = vec!["E316"];
    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e207_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let result = parser.parse_chat_file("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Child\n@ID:\teng|corpus|CHI|||||Child|||\n*CHI:\thello [@ xyz] world .\n@End");

    let errors = match result {
        Ok(_) => return Err(talkbank_parser_tests::test_error::TestError::Failure("Expected parse error but parsing succeeded".to_string())),
        Err(errors) => errors,
    };

    let expected_codes = vec!["E207"];
    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e208_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let result = parser.parse_chat_file("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|||||Target_Child|||\n*CHI:\thello [: ] .\n@End");

    let errors = match result {
        Ok(_) => return Err(talkbank_parser_tests::test_error::TestError::Failure("Expected parse error but parsing succeeded".to_string())),
        Err(errors) => errors,
    };

    let expected_codes = vec!["E376"];
    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e209_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|||||Target_Child|||\n*CHI:\t(the) dog .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E209"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e210_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Child\n@ID:\teng|corpus|CHI|||||Child|||\n*CHI:\t&+fri [: friend] world .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E387"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}

#[ignore = "Status: not_implemented (E212)"]
/// Tests expected behavior.
#[test]
fn test_e212_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Child\n@ID:\teng|corpus|CHI|||||Child|||\n@Comment:\tNote: This is a parser error, hard to trigger with valid grammar\n*CHI:\thello world .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E212"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e213_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Child\n@ID:\teng|corpus|CHI|||||Child|||\n*CHI:\thelo [: xxx] world .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E391"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}

#[ignore = "Status: not_implemented (E214)"]
/// Tests expected behavior.
#[test]
fn test_e214_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|||||Target_Child|||\n@Comment:\tERROR: Scoped annotation cannot be empty\n@Comment:\tInvalid: 'hello [*]' - Empty error annotation\n*CHI:\thello [*] .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E214"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e220_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|||||Target_Child|||\n*CHI:\thello3 .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E220"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e220_auto_utf8_begin_languages_1() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\tspa\n@Participants:\tCHI Target_Child\n@ID:\tspa|corpus|CHI|||||Target_Child|||\n*CHI:\thola2 .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E220"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e230_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|||||Target_Child|||\n@Comment:\tERROR: Compound delimiter (∆) must be balanced\n@Comment:\tInvalid: 'hello∆world' - Missing closing ∆\n*CHI:\thello∆world .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E230"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e231_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let result = parser.parse_chat_file("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Child\n@ID:\teng|corpus|CHI|||||Child|||\n*CHI:\thel(o .\n@End");

    let errors = match result {
        Ok(_) => return Err(talkbank_parser_tests::test_error::TestError::Failure("Expected parse error but parsing succeeded".to_string())),
        Err(errors) => errors,
    };

    let expected_codes = vec!["E316"];
    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e232_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|||||Target_Child|||\n@Comment:\tERROR: Compound marker (+) cannot be at word start\n@Comment:\tInvalid: '+hello' - Compound marker at start\n*CHI:\t+hello .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E316"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e233_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|||||Target_Child|||\n@Comment:\tERROR: Adjacent compound markers create empty compound part\n@Comment:\tInvalid: 'un++do' - Empty part between compound markers\n*CHI:\tun++do .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E233"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e241_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|||||Target_Child|||\n@Comment:\tERROR: Untranscribed markers must be xxx, yyy, or www\n@Comment:\tInvalid: 'xx' - Only xxx, yyy, www are allowed\n*CHI:\txx .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E241"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e241_illegal_untranscribed_marker_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|||||Target_Child|||\n*CHI:\tI said xx today .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E241"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e242_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|||||Target_Child|||\n@Comment:\tERROR: Quotation marks must be balanced\n@Comment:\tInvalid: '\"hello' - Missing closing quote\n*CHI:\t\"hello .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E316"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e243_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|||||Target_Child|||\n@Comment:\tERROR: @ character only allowed for form markers\n@Comment:\tInvalid: 'hell@' - @ in wrong position\n*CHI:\thell@ .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E202"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e244_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|||||Target_Child|||\n@Comment:\tERROR: Stress markers cannot be consecutive\n@Comment:\tInvalid: 'ˈˈhello' - Two stress marks in a row\n*CHI:\tˈˈhello .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E244"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e245_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let result = parser.parse_chat_file("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|||||Target_Child|||\n*CHI:\tˈ .\n@End");

    let errors = match result {
        Ok(_) => return Err(talkbank_parser_tests::test_error::TestError::Failure("Expected parse error but parsing succeeded".to_string())),
        Err(errors) => errors,
    };

    let expected_codes = vec!["E245"];
    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}

#[ignore = "Status: not_implemented (E246)"]
/// Tests expected behavior.
#[test]
fn test_e246_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|||||Target_Child|||\n@Comment:\tERROR: Lengthening (:) must follow spoken material\n@Comment:\tInvalid: ':hello' - Lengthening before text\n*CHI:\t:hello .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E246"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e247_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|||||Target_Child|||\n@Comment:\tERROR: Word can only have one primary stress\n@Comment:\tInvalid: 'ˈheˈllo' - Two primary stress markers\n*CHI:\tˈheˈllo .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E247"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e248_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng, spa, zho\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|||||Target_Child|||\n*CHI:\t[- zho] ni3hao3@s .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E248"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e249_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|||||Target_Child|||\n*CHI:\thello@s .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E249"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e250_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|||||Target_Child|||\n@Comment:\tERROR: Secondary stress requires primary stress\n@Comment:\tInvalid: 'ˌhello' - Secondary stress without primary\n*CHI:\tˌhello .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E250"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}

#[ignore = "Status: not_implemented (E251)"]
/// Tests expected behavior.
#[test]
fn test_e251_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let result = parser.parse_chat_file("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|||||Target_Child|||\n*CHI:\t@s:eng .\n@End");

    let errors = match result {
        Ok(_) => return Err(talkbank_parser_tests::test_error::TestError::Failure("Expected parse error but parsing succeeded".to_string())),
        Err(errors) => errors,
    };

    let expected_codes = vec!["E251"];
    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}

#[ignore = "Status: not_implemented (E252)"]
/// Tests expected behavior.
#[test]
fn test_e252_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Child\n@ID:\teng|corpus|CHI|||||Child|||\n@Comment:\tCaret for pause between syllables must appear MID-WORD (e.g., rhi^noceros)\n@Comment:\tnot at the start of a word\n*CHI:\t^test .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E252"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e253_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|||||Target_Child|||\n@Comment:\tERROR: Word must have content\n@Comment:\tInvalid: Empty word element\n*CHI:\t  .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E305"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e258_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|||||Target_Child|||\n*CHI:\thello ,, world .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E258"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e259_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|||||Target_Child|||\n*CHI:\t&=coughs , sure .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E259"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e259_auto_utf8_begin_languages_1() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|||||Target_Child|||\n*CHI:\t&=coughs, sure .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E259"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e259_auto_utf8_begin_languages_2() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|||||Target_Child|||\n*CHI:\t&-ah , more .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E259"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e259_auto_utf8_begin_languages_3() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|||||Target_Child|||\n*CHI:\t&-ah, more .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E259"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e259_auto_utf8_begin_languages_4() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|||||Target_Child|||\n*CHI:\t&~ah , more .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E259"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e259_auto_utf8_begin_languages_5() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|||||Target_Child|||\n*CHI:\t&~ah, more .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E259"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e259_auto_utf8_begin_languages_6() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|||||Target_Child|||\n*CHI:\twww , the rest .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E259"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e259_auto_utf8_begin_languages_7() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|||||Target_Child|||\n*CHI:\twww, the rest .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E259"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e301_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let result = parser.parse_chat_file("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Child\n@ID:\teng|corpus|CHI|||||Child|||\n*:\thello world .\n@End");

    let errors = match result {
        Ok(_) => return Err(talkbank_parser_tests::test_error::TestError::Failure("Expected parse error but parsing succeeded".to_string())),
        Err(errors) => errors,
    };

    let expected_codes = vec!["E301"];
    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}

#[ignore = "Status: not_implemented (E302)"]
/// Tests expected behavior.
#[test]
fn test_e302_auto_begin_languages_eng_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let result = parser.parse_chat_file("@Begin\n@Languages:\teng\n@Participants:\tCHI Child\n@ID:\teng|corpus|CHI|||||Child|||\n*ch:\thello .\n@End");

    let errors = match result {
        Ok(_) => return Err(talkbank_parser_tests::test_error::TestError::Failure("Expected parse error but parsing succeeded".to_string())),
        Err(errors) => errors,
    };

    let expected_codes = vec!["E501", "E502", "E503", "E504", "E505"];
    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}

#[ignore = "Status: not_implemented (E303)"]
/// Tests expected behavior.
#[test]
fn test_e303_auto_begin_languages_eng_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let result = parser.parse_chat_file("@Begin\n@Languages:\teng\n@Participants:\tCHI Child\n@ID:\teng|corpus|CHI|||||Child|||\n@Comment:\tNote: This may need adjustment after testing\n*CHI:\thello {{{ world }}} .\n@End");

    let errors = match result {
        Ok(_) => return Err(talkbank_parser_tests::test_error::TestError::Failure("Expected parse error but parsing succeeded".to_string())),
        Err(errors) => errors,
    };

    let expected_codes = vec!["E303"];
    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e304_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Child\n@ID:\teng|corpus|CHI|||||Child|||\n*CHI:\thello world\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E304"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e305_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let result = parser.parse_chat_file("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Child\n@ID:\teng|corpus|CHI|||||Child|||\n*CHI:\n@End");

    let errors = match result {
        Ok(_) => return Err(talkbank_parser_tests::test_error::TestError::Failure("Expected parse error but parsing succeeded".to_string())),
        Err(errors) => errors,
    };

    let expected_codes = vec!["E305"];
    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e306_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Child\n@ID:\teng|corpus|CHI|||||Child|||\n*CHI:\t.\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E306"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e307_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let result = parser.parse_chat_file("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tA:B Child\n@ID:\teng|corpus|A:B|||||Child|||\n*A:B:\thello .\n@End");

    let errors = match result {
        Ok(_) => return Err(talkbank_parser_tests::test_error::TestError::Failure("Expected parse error but parsing succeeded".to_string())),
        Err(errors) => errors,
    };

    let expected_codes = vec!["E370"];
    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e308_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Child\n@ID:\teng|corpus|CHI|||||Child|||\n*CH-I:\thello world .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E308"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e308_auto_utf8_begin_languages_1() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Child\n@ID:\teng|corpus|CHI|||||Child|||\n*MOT:\thello world .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E308"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}

#[ignore = "Status: not_implemented (E309)"]
/// Tests expected behavior.
#[test]
fn test_e309_auto_begin_languages_eng_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let result = parser.parse_chat_file("@Begin\n@Languages:\teng\n@Participants:\tCHI Child\n@ID:\teng|corpus|CHI|||||Child|||\n*CHI:\thello ## world .\n@End");

    let errors = match result {
        Ok(_) => return Err(talkbank_parser_tests::test_error::TestError::Failure("Expected parse error but parsing succeeded".to_string())),
        Err(errors) => errors,
    };

    let expected_codes = vec!["E501", "E502", "E503", "E504", "E505"];
    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}

#[ignore = "Status: not_implemented (E310)"]
/// Tests expected behavior.
#[test]
fn test_e310_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Child\n@ID:\teng|corpus|CHI|||||Child|||\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E310"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}

#[ignore = "Status: not_implemented (E311)"]
/// Tests expected behavior.
#[test]
fn test_e311_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let result = parser.parse_chat_file("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Child\n@ID:\teng|corpus|CHI|||||Child|||\n*CHI:\t[: unclosed replacement [* error] .\n@End");

    let errors = match result {
        Ok(_) => return Err(talkbank_parser_tests::test_error::TestError::Failure("Expected parse error but parsing succeeded".to_string())),
        Err(errors) => errors,
    };

    let expected_codes = vec!["E316"];
    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}

#[ignore = "Status: not_implemented (E312)"]
/// Tests expected behavior.
#[test]
fn test_e312_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let result = parser.parse_chat_file("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Child\n@ID:\teng|corpus|CHI|||||Child|||\n*CHI:\tword [= comment .\n@End");

    let errors = match result {
        Ok(_) => return Err(talkbank_parser_tests::test_error::TestError::Failure("Expected parse error but parsing succeeded".to_string())),
        Err(errors) => errors,
    };

    let expected_codes = vec!["E304", "E375"];
    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e313_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let result = parser.parse_chat_file("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Child\n@ID:\teng|corpus|CHI|||||Child|||\n*CHI:\thel(lo .\n@End");

    let errors = match result {
        Ok(_) => return Err(talkbank_parser_tests::test_error::TestError::Failure("Expected parse error but parsing succeeded".to_string())),
        Err(errors) => errors,
    };

    let expected_codes = vec!["E316"];
    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e314_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let result = parser.parse_chat_file("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Child\n@ID:\teng|corpus|CHI|||||Child|||\n*CHI:\tword [\n@End");

    let errors = match result {
        Ok(_) => return Err(talkbank_parser_tests::test_error::TestError::Failure("Expected parse error but parsing succeeded".to_string())),
        Err(errors) => errors,
    };

    let expected_codes = vec!["E375"];
    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e315_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let result = parser.parse_chat_file("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Child\n@ID:\teng|corpus|CHI|||||Child|||\n*CHI:\tword\u{1}test .\n@End");

    let errors = match result {
        Ok(_) => return Err(talkbank_parser_tests::test_error::TestError::Failure("Expected parse error but parsing succeeded".to_string())),
        Err(errors) => errors,
    };

    let expected_codes = vec!["E316"];
    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e316_angle_bracket_in_mor_stem_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let result = parser.parse_chat_file("@UTF8\n@Begin\n@Languages:\tfin\n@Participants:\tCHI Target_Child\n@ID:\tfin|test|CHI|||||Target_Child|||\n*CHI:\tkato tos .\n%mor:\tintj|kato noun|<sos>tos .\n@End");

    let errors = match result {
        Ok(_) => return Err(talkbank_parser_tests::test_error::TestError::Failure("Expected parse error but parsing succeeded".to_string())),
        Err(errors) => errors,
    };

    let expected_codes = vec!["E316"];
    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e316_angle_bracket_in_mor_stem_utf8_begin_languages_1() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let result = parser.parse_chat_file("@UTF8\n@Begin\n@Languages:\tfin\n@Participants:\tCHI Target_Child\n@ID:\tfin|test|CHI|||||Target_Child|||\n*CHI:\ttos ei .\n%mor:\tsconj|<sos>tos~aux|ei-Fin-Neg-S3 .\n@End");

    let errors = match result {
        Ok(_) => return Err(talkbank_parser_tests::test_error::TestError::Failure("Expected parse error but parsing succeeded".to_string())),
        Err(errors) => errors,
    };

    let expected_codes = vec!["E316"];
    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e316_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let result = parser.parse_chat_file("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Child, MOT Mother\n@ID:\teng|corpus|CHI|||||Child|||\n@ID:\teng|corpus|MOT|||||Mother|||\n*CHI:\thello . [+ bch] 2041689_2042652\n*CHI:\tworld . [+ bch] 2051689_2052652\n@End");

    let errors = match result {
        Ok(_) => return Err(talkbank_parser_tests::test_error::TestError::Failure("Expected parse error but parsing succeeded".to_string())),
        Err(errors) => errors,
    };

    let expected_codes = vec!["E316"];
    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e316_auto_utf8_begin_languages_1() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let result = parser.parse_chat_file("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Child\n@ID:\teng|corpus|CHI|||||Child|||\n@Comment:\tNote: This may need adjustment after testing\n*CHI:\thello {{{ world }}} .\n@End");

    let errors = match result {
        Ok(_) => return Err(talkbank_parser_tests::test_error::TestError::Failure("Expected parse error but parsing succeeded".to_string())),
        Err(errors) => errors,
    };

    let expected_codes = vec!["E316"];
    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e316_auto_utf8_begin_languages_2() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let result = parser.parse_chat_file("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Child\n@ID:\teng|corpus|CHI|||||Child|||\n@Comment:\tNote: This may need adjustment after testing\n*CHI:\t<<< [= test] >>> .\n@End");

    let errors = match result {
        Ok(_) => return Err(talkbank_parser_tests::test_error::TestError::Failure("Expected parse error but parsing succeeded".to_string())),
        Err(errors) => errors,
    };

    let expected_codes = vec!["E316"];
    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e316_auto_utf8_begin_languages_3() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let result = parser.parse_chat_file("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Child\n@ID:\teng|corpus|CHI|||||Child|||\n@Comment:\tNote: This may need adjustment after testing\n*CHI:\t<<<<< hello >>>>> world .\n@End");

    let errors = match result {
        Ok(_) => return Err(talkbank_parser_tests::test_error::TestError::Failure("Expected parse error but parsing succeeded".to_string())),
        Err(errors) => errors,
    };

    let expected_codes = vec!["E316"];
    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e316_auto_utf8_begin_languages_4() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let result = parser.parse_chat_file("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Child\n@ID:\teng|corpus|CHI|||||Child|||\n*CHI\thello world .\n@End");

    let errors = match result {
        Ok(_) => return Err(talkbank_parser_tests::test_error::TestError::Failure("Expected parse error but parsing succeeded".to_string())),
        Err(errors) => errors,
    };

    let expected_codes = vec!["E316"];
    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e316_auto_utf8_begin_begin_5() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let result = parser.parse_chat_file("@UTF8\n@Begin\n@Begin\n@Languages:\teng\n@Participants:\tCHI Child\n@ID:\teng|corpus|CHI|||||Child|||\n@End");

    let errors = match result {
        Ok(_) => return Err(talkbank_parser_tests::test_error::TestError::Failure("Expected parse error but parsing succeeded".to_string())),
        Err(errors) => errors,
    };

    let expected_codes = vec!["E501"];
    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e316_auto_utf8_begin_languages_6() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let result = parser.parse_chat_file("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Child\n@ID:\teng|corpus|CHI|||||Child|||\n@Comment:\tNote: Timestamp shows 2052652_2041689 where start > end\n*CHI:\thello world . [+ bch] 2052652_2041689\n@End");

    let errors = match result {
        Ok(_) => return Err(talkbank_parser_tests::test_error::TestError::Failure("Expected parse error but parsing succeeded".to_string())),
        Err(errors) => errors,
    };

    let expected_codes = vec!["E316"];
    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e316_auto_utf8_begin_languages_7() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let result = parser.parse_chat_file("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Child\n@ID:\teng|corpus|CHI|||||Child|||\n*CHI:\thello world .\n%mor:\thello n|world .\n@End");

    let errors = match result {
        Ok(_) => return Err(talkbank_parser_tests::test_error::TestError::Failure("Expected parse error but parsing succeeded".to_string())),
        Err(errors) => errors,
    };

    let expected_codes = vec!["E316"];
    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}

#[ignore = "Status: not_implemented (E319)"]
/// Tests expected behavior.
#[test]
fn test_e319_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let result = parser.parse_chat_file("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|test|CHI||female|||Target_Child|||\n*CHI:\thello .\n%%%broken line content here\n@End");

    let errors = match result {
        Ok(_) => return Err(talkbank_parser_tests::test_error::TestError::Failure("Expected parse error but parsing succeeded".to_string())),
        Err(errors) => errors,
    };

    let expected_codes = vec!["E319"];
    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}

#[ignore = "Status: not_implemented (E320)"]
/// Tests expected behavior.
#[test]
fn test_e320_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let result = parser.parse_chat_file("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|test|CHI||female|||Target_Child|||\n@Transcription:\t[malformed content with [unbalanced brackets\n*CHI:\thello .\n@End");

    let errors = match result {
        Ok(_) => return Err(talkbank_parser_tests::test_error::TestError::Failure("Expected parse error but parsing succeeded".to_string())),
        Err(errors) => errors,
    };

    let expected_codes = vec!["E320"];
    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}

#[ignore = "Status: not_implemented (E321)"]
/// Tests expected behavior.
#[test]
fn test_e321_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let result = parser.parse_chat_file("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|test|CHI||female|||Target_Child|||\n*CHI:\thello [% broken [nested unclosed .\n@End");

    let errors = match result {
        Ok(_) => return Err(talkbank_parser_tests::test_error::TestError::Failure("Expected parse error but parsing succeeded".to_string())),
        Err(errors) => errors,
    };

    let expected_codes = vec!["E321"];
    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}

#[ignore = "Status: not_implemented (E322)"]
/// Tests expected behavior.
#[test]
fn test_e322_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let result = parser.parse_chat_file("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|test|CHI||female|||Target_Child|||\n*CHI hello .\n@End");

    let errors = match result {
        Ok(_) => return Err(talkbank_parser_tests::test_error::TestError::Failure("Expected parse error but parsing succeeded".to_string())),
        Err(errors) => errors,
    };

    let expected_codes = vec!["E322"];
    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}

#[ignore = "Status: not_implemented (E323)"]
/// Tests expected behavior.
#[test]
fn test_e323_auto_begin_languages_eng_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let result = parser.parse_chat_file("@Begin\n@Languages:\teng\n@Participants:\tCHI Child\n@ID:\teng|corpus|CHI|||||Child|||\n@Comment:\tERROR: Speaker must be followed by colon\n@Comment:\tInvalid: '*CHI hello' - Missing colon\n*CHI hello .\n@End");

    let errors = match result {
        Ok(_) => return Err(talkbank_parser_tests::test_error::TestError::Failure("Expected parse error but parsing succeeded".to_string())),
        Err(errors) => errors,
    };

    let expected_codes = vec!["E316", "E502", "E503", "E504", "E505"];
    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e324_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let result = parser.parse_chat_file("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Child\n@ID:\teng|corpus|CHI|||||Child|||\n*CHI:\thello } .\n@End");

    let errors = match result {
        Ok(_) => return Err(talkbank_parser_tests::test_error::TestError::Failure("Expected parse error but parsing succeeded".to_string())),
        Err(errors) => errors,
    };

    let expected_codes = vec!["E316"];
    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}

#[ignore = "Status: not_implemented (E325)"]
/// Tests expected behavior.
#[test]
fn test_e325_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let result = parser.parse_chat_file("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|test|CHI||female|||Target_Child|||\n*CHI:\thello .\n@@@unexpected content\n%mor:\tco|hello .\n@End");

    let errors = match result {
        Ok(_) => return Err(talkbank_parser_tests::test_error::TestError::Failure("Expected parse error but parsing succeeded".to_string())),
        Err(errors) => errors,
    };

    let expected_codes = vec!["E325"];
    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e326_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|test|CHI||female|||Target_Child|||\n*CHI:\thello .\nthis is just a bare line of text\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E326"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e330_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let result = parser.parse_chat_file("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Child\n@ID:\teng|corpus|CHI|||||Child|||\n*CHI:\t<hello [/] .\n@End");

    let errors = match result {
        Ok(_) => return Err(talkbank_parser_tests::test_error::TestError::Failure("Expected parse error but parsing succeeded".to_string())),
        Err(errors) => errors,
    };

    let expected_codes = vec!["E316"];
    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}

#[ignore = "Status: not_implemented (E331)"]
/// Tests expected behavior.
#[test]
fn test_e331_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let result = parser.parse_chat_file("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tMAR Target_Child\n@ID:\teng|corpus|MAR|||||Target_Child|||\n*MAR:\tdon't give me a YM . 1520652_1522904\n%mor:\taux|do-Fin-Imp-S~part|not verb|give-Fin-Imp-S pron|I-Prs-Acc-S1 det|a-Ind-Art noun|-Acc .\n%gra:\t1|3|AUX 2|3|ADVMOD 3|6|ROOT 4|3|IOBJ 5|6|DET 6|3|OBJ 7|3|PUNCT\n@End");

    let errors = match result {
        Ok(_) => return Err(talkbank_parser_tests::test_error::TestError::Failure("Expected parse error but parsing succeeded".to_string())),
        Err(errors) => errors,
    };

    let expected_codes = vec!["E331"];
    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}

#[ignore = "Status: not_implemented (E341)"]
/// Tests expected behavior.
#[test]
fn test_e341_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tMOT Mother, CHI Target_Child\n@ID:\teng|test|MOT||female|||Mother|||\n@ID:\teng|test|CHI||male|||Target_Child|||\n*MOT:\tshe said +\"/.\n*CHI:\twhat .\n*MOT:\tgo to bed .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E341"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}

#[ignore = "Status: not_implemented (E341)"]
/// Tests expected behavior.
#[test]
fn test_e341_auto_utf8_begin_languages_1() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tMOT Mother\n@ID:\teng|test|MOT||female|||Mother|||\n*MOT:\tshe said +\"/.\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E341"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}

#[ignore = "Status: not_implemented (E342)"]
/// Tests expected behavior.
#[test]
fn test_e342_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let result = parser.parse_chat_file("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Child\n@ID:\teng|corpus|CHI|||||Child|||\n*CHI:\thelo [: 0] world .\n@End");

    let errors = match result {
        Ok(_) => return Err(talkbank_parser_tests::test_error::TestError::Failure("Expected parse error but parsing succeeded".to_string())),
        Err(errors) => errors,
    };

    let expected_codes = vec!["E390"];
    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}

#[ignore = "Status: not_implemented (E342)"]
/// Tests expected behavior.
#[test]
fn test_e342_auto_utf8_begin_languages_1() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let result = parser.parse_chat_file("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Child\n@ID:\teng|corpus|CHI|||||Child|||\n*CHI:\thello world .\n%mor:\t|hello n|world .\n@End");

    let errors = match result {
        Ok(_) => return Err(talkbank_parser_tests::test_error::TestError::Failure("Expected parse error but parsing succeeded".to_string())),
        Err(errors) => errors,
    };

    let expected_codes = vec!["E316", "E702"];
    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}

#[ignore = "Status: not_implemented (E342)"]
/// Tests expected behavior.
#[test]
fn test_e342_auto_utf8_begin_languages_2() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let result = parser.parse_chat_file("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Child\n@ID:\teng|corpus|CHI|||||Child|||\n*CHI:\thello world .\n%mor:\tv| n|world .\n@End");

    let errors = match result {
        Ok(_) => return Err(talkbank_parser_tests::test_error::TestError::Failure("Expected parse error but parsing succeeded".to_string())),
        Err(errors) => errors,
    };

    let expected_codes = vec!["E316"];
    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}

#[ignore = "Status: not_implemented (E342)"]
/// Tests expected behavior.
#[test]
fn test_e342_auto_utf8_begin_languages_3() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let result = parser.parse_chat_file("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Child\n@ID:\teng|corpus|CHI|||||Child|||\n*CHI:\thello world .\n%gra:\t1|2| 2|0|ROOT\n@End");

    let errors = match result {
        Ok(_) => return Err(talkbank_parser_tests::test_error::TestError::Failure("Expected parse error but parsing succeeded".to_string())),
        Err(errors) => errors,
    };

    let expected_codes = vec!["E316"];
    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}

#[ignore = "Status: not_implemented (E344)"]
/// Tests expected behavior.
#[test]
fn test_e344_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let result = parser.parse_chat_file("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|||||Target_Child|||\n*CHI:\thello <world <foo> bar> .\n@End");

    let errors = match result {
        Ok(_) => return Err(talkbank_parser_tests::test_error::TestError::Failure("Expected parse error but parsing succeeded".to_string())),
        Err(errors) => errors,
    };

    let expected_codes = vec!["E316"];
    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}

#[ignore = "Status: not_implemented (E346)"]
/// Tests expected behavior.
#[test]
fn test_e346_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let result = parser.parse_chat_file("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|||||Target_Child|||\n*CHI:\thello world> [/] .\n@End");

    let errors = match result {
        Ok(_) => return Err(talkbank_parser_tests::test_error::TestError::Failure("Expected parse error but parsing succeeded".to_string())),
        Err(errors) => errors,
    };

    let expected_codes = vec!["E316"];
    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e347_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Child, MOT Mother\n@ID:\teng|corpus|CHI|||||Child|||\n@ID:\teng|corpus|MOT|||||Mother|||\n*CHI:\t⌈2hello⌉2 .\n*MOT:\thi .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E347"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}

#[ignore = "Status: not_implemented (E348)"]
/// Tests expected behavior.
#[test]
fn test_e348_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child, MOT Mother\n@ID:\teng|corpus|CHI|||||Target_Child|||\n@ID:\teng|corpus|MOT|||||Mother|||\n*CHI:\thello ⌈ world .\n*MOT:\tyes .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E348"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}

#[ignore = "Status: not_implemented (E351)"]
/// Tests expected behavior.
#[test]
fn test_e351_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|test|CHI||male|||Target_Child|||\n*CHI:\t+, hello .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E351"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}

#[ignore = "Status: not_implemented (E352)"]
/// Tests expected behavior.
#[test]
fn test_e352_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|test|CHI||male|||Target_Child|||\n*CHI:\tI want a cookie .\n*CHI:\t+, actually ice cream .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E352"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}

#[ignore = "Status: not_implemented (E353)"]
/// Tests expected behavior.
#[test]
fn test_e353_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|test|CHI||male|||Target_Child|||\n*CHI:\t++ hello .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E353"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}

#[ignore = "Status: not_implemented (E354)"]
/// Tests expected behavior.
#[test]
fn test_e354_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tMOT Mother, CHI Target_Child\n@ID:\teng|test|MOT||female|||Mother|||\n@ID:\teng|test|CHI||male|||Target_Child|||\n*MOT:\tgo to bed .\n*CHI:\t++ no I won't .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E354"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}

#[ignore = "Status: not_implemented (E355)"]
/// Tests expected behavior.
#[test]
fn test_e355_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|test|CHI||male|||Target_Child|||\n*CHI:\tI was going +...\n*CHI:\t++ to say hello .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E355"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e356_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|||||Target_Child|||\n@Comment:\tERROR: Unmatched underline begin marker\n*CHI:\thello \u{2}\u{1}world .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E356"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e357_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|||||Target_Child|||\n@Comment:\tERROR: Unmatched underline end marker\n*CHI:\thello \u{2}\u{2}world .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E357"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e358_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|test|CHI||||Target_Child|||\n*CHI:\tI am &{l=singing happy birthday to you .\n*CHI:\tanother utterance .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E358"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e359_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|test|CHI||||Target_Child|||\n*CHI:\thappy birthday to you &}l=singing .\n*CHI:\tanother utterance .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E359"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}

#[ignore = "Status: not_implemented (E360)"]
/// Tests expected behavior.
#[test]
fn test_e360_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let result = parser.parse_chat_file("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Child\n@ID:\teng|corpus|CHI|||||Child|||\n*CHI:\thello . 0_0\n@End");

    let errors = match result {
        Ok(_) => return Err(talkbank_parser_tests::test_error::TestError::Failure("Expected parse error but parsing succeeded".to_string())),
        Err(errors) => errors,
    };

    let expected_codes = vec!["E362"];
    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}

#[ignore = "Status: not_implemented (E360)"]
/// Tests expected behavior.
#[test]
fn test_e360_deprecated_skip_bullet_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Child\n@ID:\teng|corpus|CHI|||||Child|||\n*CHI:\thello . 357000_357477-\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E316"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e361_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let result = parser.parse_chat_file("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Child\n@ID:\teng|corpus|CHI|||||Child|||\n*CHI:\thello [^ bad[ .\n@End");

    let errors = match result {
        Ok(_) => return Err(talkbank_parser_tests::test_error::TestError::Failure("Expected parse error but parsing succeeded".to_string())),
        Err(errors) => errors,
    };

    let expected_codes = vec!["E316"];
    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e362_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|2;6|male|||Target_Child|||\n@Media:\tsample, audio\n*CHI:\thello there . \u{15}10000_12000\u{15}\n@Comment:\tFirst bullet: 10000-12000ms\n*CHI:\thow are you . \u{15}8000_9000\u{15}\n@Comment:\tERROR: Second bullet starts at 8000ms, which is BEFORE the first bullet\n@Comment:\tTimestamps must be monotonically increasing\n*CHI:\tI am fine . \u{15}15000_17000\u{15}\n@Comment:\tVALID: 15000 > 12000\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E362"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e363_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let result = parser.parse_chat_file("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Child\n@ID:\teng|corpus|CHI|||||Child|||\n*CHI:\thello . [+ ]\n@End");

    let errors = match result {
        Ok(_) => return Err(talkbank_parser_tests::test_error::TestError::Failure("Expected parse error but parsing succeeded".to_string())),
        Err(errors) => errors,
    };

    let expected_codes = vec!["E316"];
    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}

#[ignore = "Status: not_implemented (E364)"]
/// Tests expected behavior.
#[test]
fn test_e364_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let result = parser.parse_chat_file("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Child\n@ID:\teng|corpus|CHI|||||Child|||\n*CHI:\thello@s:+ .\n@End");

    let errors = match result {
        Ok(_) => return Err(talkbank_parser_tests::test_error::TestError::Failure("Expected parse error but parsing succeeded".to_string())),
        Err(errors) => errors,
    };

    let expected_codes = vec!["E246", "E249"];
    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}

#[ignore = "Status: not_implemented (E365)"]
/// Tests expected behavior.
#[test]
fn test_e365_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Child\n@ID:\teng|corpus|CHI|||||Child|||\n*CHI:\thello .\n%pho:\t***bad***\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E365"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e367_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|test|CHI||||Target_Child|||\n*CHI:\t&{n=crying I want mommy .\n*CHI:\tplease .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E367"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e368_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|test|CHI||||Target_Child|||\n*CHI:\tI want mommy &}n=crying .\n*CHI:\tplease .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E368"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}

#[ignore = "Status: not_implemented (E370)"]
/// Tests expected behavior.
#[test]
fn test_e370_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|2;6|male|||Target_Child|||\n*CHI:\tI <wanna> [: want to] <the> [: a] cookie .\n%mor:\tpro|I v|want n|cookie .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E316", "E600"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}

#[ignore = "Status: not_implemented (E370)"]
/// Tests expected behavior.
#[test]
fn test_e370_auto_utf8_begin_languages_1() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|2;6|male|||Target_Child|||\n*CHI:\tI <want the> [: need the] cookie .\n%mor:\tpro|I v|need n|cookie .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E316"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}

#[ignore = "Status: not_implemented (E370)"]
/// Tests expected behavior.
#[test]
fn test_e370_auto_utf8_begin_languages_2() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|2;6|male|||Target_Child|||\n*CHI:\tI <want the big> [: need a large] cookie .\n%mor:\tpro|I v|need n|cookie .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E316"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e371_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|2;6|male|||Target_Child|||\n*CHI:\thello ‹hɛ (.) loʊ› there .\n@Comment:\tERROR: Pause (.) cannot be embedded inside phonological group ‹...›\n*CHI:\tgoodbye ‹gʊd baɪ› friend .\n@Comment:\tVALID: No pause inside ‹...›\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E371"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e372_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child, MOT Mother\n@ID:\teng|corpus|CHI|2;6|male|||Target_Child|||\n@ID:\teng|corpus|MOT|30;0|female|||Mother|||\n*MOT:\tshe said “I told him “go away” yesterday” .\n@Comment:\tERROR: Nested quotations - \"go away\" is inside \"I told him...\"\n@Comment:\tJava detects this with state stack; Rust only counts balance\n*CHI:\tokay mommy .\n*MOT:\the said “hello” and “goodbye” .\n@Comment:\tVALID: Two separate quotations, not nested\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E372"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e373_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|||||Target_Child|||\n@Comment:\tERROR: Overlap index must be 2-9\n*CHI:\t⌈1 hello ⌉ .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E373"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e373_auto_utf8_begin_languages_1() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tMOT Mother, CHI Target_Child\n@ID:\teng|test|MOT||female|||Mother|||\n@ID:\teng|test|CHI||male|||Target_Child|||\n*MOT:\tI think [<0] so .\n*CHI:\tyeah [>0] .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E316"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e375_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let result = parser.parse_chat_file("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Child\n@ID:\teng|corpus|CHI|||||Child|||\n@Comment:\tNote: This may need adjustment after testing\n*CHI:\thello [[[[ test ]]]] world .\n@End");

    let errors = match result {
        Ok(_) => return Err(talkbank_parser_tests::test_error::TestError::Failure("Expected parse error but parsing succeeded".to_string())),
        Err(errors) => errors,
    };

    let expected_codes = vec!["E375"];
    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e376_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let result = parser.parse_chat_file("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Child\n@ID:\teng|corpus|CHI|||||Child|||\n*CHI:\thello [:] world .\n@End");

    let errors = match result {
        Ok(_) => return Err(talkbank_parser_tests::test_error::TestError::Failure("Expected parse error but parsing succeeded".to_string())),
        Err(errors) => errors,
    };

    let expected_codes = vec!["E376"];
    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e382_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let result = parser.parse_chat_file("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Child\n@ID:\teng|corpus|CHI|||||Child|||\n*CHI:\thello world .\n%mor:\t|hello n|world .\n@End");

    let errors = match result {
        Ok(_) => return Err(talkbank_parser_tests::test_error::TestError::Failure("Expected parse error but parsing succeeded".to_string())),
        Err(errors) => errors,
    };

    let expected_codes = vec!["E316", "E702"];
    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e382_auto_utf8_begin_languages_1() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let result = parser.parse_chat_file("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Child\n@ID:\teng|corpus|CHI|||||Child|||\n*CHI:\thello world .\n%mor:\thello n|world .\n@End");

    let errors = match result {
        Ok(_) => return Err(talkbank_parser_tests::test_error::TestError::Failure("Expected parse error but parsing succeeded".to_string())),
        Err(errors) => errors,
    };

    let expected_codes = vec!["E316"];
    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e382_auto_utf8_begin_languages_2() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let result = parser.parse_chat_file("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Child\n@ID:\teng|corpus|CHI|||||Child|||\n*CHI:\thello world .\n%mor:\tv| n|world .\n@End");

    let errors = match result {
        Ok(_) => return Err(talkbank_parser_tests::test_error::TestError::Failure("Expected parse error but parsing succeeded".to_string())),
        Err(errors) => errors,
    };

    let expected_codes = vec!["E316"];
    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e387_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Child\n@ID:\teng|corpus|CHI|||||Child|||\n*CHI:\t&+ba [: dog] .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E387"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e388_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Child\n@ID:\teng|corpus|CHI|||||Child|||\n*CHI:\t&=laugh [: well] .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E316"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e389_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Child\n@ID:\teng|corpus|CHI|||||Child|||\n*CHI:\t&-um [: and] .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E389"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e390_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Child\n@ID:\teng|corpus|CHI|||||Child|||\n*CHI:\twent [: 0go] .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E390"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e391_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Child\n@ID:\teng|corpus|CHI|||||Child|||\n*CHI:\twent [: xxx] .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E391"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e401_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|2;6|male|||Target_Child|||\n*CHI:\tI want cookie .\n%mor:\tpro|I v|want n|cookie .\n%mor:\tpro|I v|want n|cookie .\n@Comment:\tERROR: Duplicate %mor tier for the same utterance\n*CHI:\tyou have ball .\n%gra:\t1|2|SUBJ 2|0|ROOT 3|2|OBJ 4|2|PUNCT\n%gra:\t1|2|SUBJ 2|0|ROOT 3|2|OBJ 4|2|PUNCT\n@Comment:\tERROR: Duplicate %gra tier for the same utterance\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E401"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e401_auto_utf8_begin_languages_1() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|2;6|male|||Target_Child|||\n*CHI:\tI want cookie .\n%mor:\tpro|I v|want n|cookie .\n%mor:\tpro|I v|want n|cookie .\n@Comment:\tERROR: Duplicate %mor tier should trigger E401\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E401"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e404_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|||||Target_Child|||\n%mor:\tco|hello .\n*CHI:\thello .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E404"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e501_auto_utf8_begin_begin_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let result = parser.parse_chat_file("@UTF8\n@Begin\n@Begin\n@Languages:\teng\n@Participants:\tCHI Child\n@ID:\teng|corpus|CHI|||||Child|||\n@End");

    let errors = match result {
        Ok(_) => return Err(talkbank_parser_tests::test_error::TestError::Failure("Expected parse error but parsing succeeded".to_string())),
        Err(errors) => errors,
    };

    let expected_codes = vec!["E501"];
    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e501_auto_utf8_begin_languages_1() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let result = parser.parse_chat_file("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI\n@ID:\teng|corpus|CHI|||||CHI|||\n@End");

    let errors = match result {
        Ok(_) => return Err(talkbank_parser_tests::test_error::TestError::Failure("Expected parse error but parsing succeeded".to_string())),
        Err(errors) => errors,
    };

    let expected_codes = vec!["E513"];
    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e502_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Child\n@ID:\teng|corpus|CHI|||||Child|||\n*CHI:\thello world .", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E502"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e503_auto_begin_languages_eng_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@Begin\n@Languages:\teng\n@Participants:\tCHI Child\n@ID:\teng|corpus|CHI|||||Child|||\n*CHI:\thello world .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E503"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e504_auto_utf8_begin_participants_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Participants:\tCHI Child\n@ID:\teng|corpus|CHI|||||Child|||\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E504"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e504_auto_utf8_begin_languages_1() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E504"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e505_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Child\n@ID:\teng|corpus\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E505"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e505_auto_utf8_begin_languages_1() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Child\n@ID:\teng|corpus||\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E505"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e505_auto_utf8_begin_languages_2() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Child\n@ID:\t|corpus|CHI|\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E505"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e506_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let result = parser.parse_chat_file("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\t\n\n@End");

    let errors = match result {
        Ok(_) => return Err(talkbank_parser_tests::test_error::TestError::Failure("Expected parse error but parsing succeeded".to_string())),
        Err(errors) => errors,
    };

    let expected_codes = vec!["E342"];
    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e507_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\n@Participants:\tCHI Child\n@ID:\teng|corpus|CHI|||||Child|||\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E507"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e508_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Child\n@ID:\teng|corpus|CHI|||||Child|||\n@Date:\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E516"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e509_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Child\n@ID:\teng|corpus|CHI|||||Child|||\n@Media:\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E509"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e510_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let result = parser.parse_chat_file("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Child\n@ID:\t|corpus|CHI|\n@End");

    let errors = match result {
        Ok(_) => return Err(talkbank_parser_tests::test_error::TestError::Failure("Expected parse error but parsing succeeded".to_string())),
        Err(errors) => errors,
    };

    let expected_codes = vec!["E505"];
    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e511_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Child\n@ID:\teng|corpus||||||Child|||\n*CHI:\thello .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E522", "E523"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e512_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tChild\n*CHI:\thello .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E513"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e513_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI\n@ID:\teng|corpus|CHI|||||CHI|||\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E513"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e515_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Child\n@ID:\teng|corpus|CHI|||||||\n*CHI:\thello .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E515"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e516_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|||||Target_Child|||\n@Date:\t\n@Comment:\tERROR: Date header cannot be empty\n*CHI:\thello .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E516"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e517_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Child\n@ID:\teng|corpus|CHI|2.6||||Child|||\n@Comment:\tShould be: \"2;6.0\" or \"2;6\" (years;months.days format)\n*CHI:\thello .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E517"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e518_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|||||Target_Child|||\n@Date:\t1-JAN-2024\n@End\n\n*CHI:\thello .", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E518"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e518_auto_utf8_begin_languages_1() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|||||Target_Child|||\n@Date:\t01/01/2024\n@End\n\n*CHI:\thello .", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E518"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e518_auto_utf8_begin_languages_2() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|||||Target_Child|||\n@Date:\t01-ABC-2024\n@End\n\n*CHI:\thello .", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E518"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e518_auto_utf8_begin_languages_3() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|||||Target_Child|||\n@Date:\t32-JAN-2024\n@End\n\n*CHI:\thello .", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E518"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e518_auto_utf8_begin_languages_4() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Child\n@ID:\teng|corpus|CHI|||||Child|||\n@Date:\t01-January-2025\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E518"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e518_auto_utf8_begin_languages_5() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|||||Target_Child|||\n@Date:\t01-jan-2024\n@End\n\n*CHI:\thello .", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E518"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e519_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\txxx\n@Participants:\tCHI Child\n@ID:\txxx|corpus|CHI|||||Child|||\n*CHI:\thello .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E519"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e522_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\t\n\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E342"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e522_auto_utf8_begin_languages_1() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Ruth Target_Child, MOT Mother\n@ID:\teng|corpus|MOT|||||Mother|||\n*CHI:\thello .\n*MOT:\thi there .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E522"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e522_undefined_participant_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|||||Target_Child|||\n*MOT:\thello dear .\n*CHI:\thi mommy .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E522"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e523_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Ruth Target_Child\n@ID:\teng|corpus|CHI|2;6.0||||Target_Child|||\n@ID:\teng|corpus|MOT|||||Mother|||\n*CHI:\thello .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E523"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e524_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Ruth Target_Child\n@ID:\teng|corpus|CHI|2;6.0||||Target_Child|||\n@Birth of MOT:\t01-JAN-2000\n*CHI:\thello .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E524"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e525_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|2;6|male|||Target_Child|||\n@Bg:test\n@Comment:\tThis is inside the first @Bg:test scope\n@Bg:test\n@Comment:\tERROR: This second @Bg:test should be invalid (nested @Bg with same label)\n@Eg:test\n@Eg:test\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E529"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e525_auto_utf8_begin_languages_1() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|2;6|male|||Target_Child|||\n@Bg:activity\n@Comment:\tWe are inside a @Bg/@Eg scope\n@G:\tplaying with blocks\n@Comment:\tERROR: @G (lazy gem) should not be allowed inside @Bg/@Eg scope\n@Eg:activity\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E530"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e525_auto_utf8_begin_languages_2() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Child\n@ID:\teng|test|CHI|||||Child|||\n@UnknownHeader:\tthis header does not exist\n*CHI:\thello .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E525"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e525_auto_utf8_begin_languages_3() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|test|CHI||||Target_Child|||\n@Bg:\tepisode1\n*CHI:\thello world .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E526"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e525_auto_utf8_begin_languages_4() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|test|CHI||||Target_Child|||\n*CHI:\thello world .\n@Eg:\tepisode1\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E527"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e526_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|test|CHI||||Target_Child|||\n@Bg:\tepisode1\n*CHI:\thello world .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E526"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e527_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|test|CHI||||Target_Child|||\n*CHI:\thello world .\n@Eg:\tepisode1\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E527"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e528_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Child\n@ID:\teng|corpus|CHI|||||Child|||\n@Bg:\tStory\n*CHI:\thello .\n@Eg:\tGame\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E528"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e529_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|2;6|male|||Target_Child|||\n@Bg:test\n@Comment:\tThis is inside the first @Bg:test scope\n@Bg:test\n@Comment:\tERROR: This second @Bg:test should be invalid (nested @Bg with same label)\n@Eg:test\n@Eg:test\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E529"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e530_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|2;6|male|||Target_Child|||\n@Bg:activity\n@Comment:\tWe are inside a @Bg/@Eg scope\n@G:\tplaying with blocks\n@Comment:\tERROR: @G (lazy gem) should not be allowed inside @Bg/@Eg scope\n@Eg:activity\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E530"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}

#[ignore = "Status: not_implemented (E531)"]
/// Tests expected behavior.
#[test]
fn test_e531_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|||||Target_Child|||\n@Media:\tdifferent, audio\n@Comment:\tERROR: Media filename must match transcript\n*CHI:\thello .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E531"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e532_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child, MOT Mother, INV Investigator, BOB InvalidRole\n@ID:\teng|corpus|CHI|2;6|male|||Target_Child|||\n@ID:\teng|corpus|MOT|30;0|female|||Mother|||\n@ID:\teng|corpus|INV|25;0|female|||Investigator|||\n@ID:\teng|corpus|BOB|35;0|male|||InvalidRole|||\n@Comment:\tERROR: \"InvalidRole\" is not a valid participant role\n@Comment:\tValid roles include: Target_Child, Mother, Father, Investigator, etc.\n*CHI:\thello .\n*MOT:\thi sweetie .\n*BOB:\tI have an invalid role .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E532"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e533_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Options:\t\n@Participants:\tCHI Child\n@ID:\teng|corpus|CHI|||||Child|||\n*CHI:\thello .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E533"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e534_unsupported_option_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|||||Target_Child|||\n@Options:\tbadoption\n*CHI:\thello world .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E534"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e535_unsupported_media_type_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|||||Target_Child|||\n@Media:\trecording, badtype\n*CHI:\thello world .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E535"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e536_unsupported_media_status_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|||||Target_Child|||\n@Media:\trecording, audio, badstatus\n*CHI:\thello world .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E536"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e537_unsupported_number_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|||||Target_Child|||\n@Number:\tbadnumber\n*CHI:\thello world .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E537"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e538_unsupported_recording_quality_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|||||Target_Child|||\n@Recording Quality:\tbadquality\n*CHI:\thello world .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E538"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e539_unsupported_transcription_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|||||Target_Child|||\n@Transcription:\tbadtype\n*CHI:\thello world .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E539"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e540_invalid_time_duration_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|||||Target_Child|||\n@Time Duration:\tnot-a-time\n*CHI:\thello world .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E540"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e541_invalid_time_start_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|||||Target_Child|||\n@Time Start:\tnot-a-time\n*CHI:\thello world .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E541"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e542_unsupported_sex_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|3;6|badsex|||Target_Child|||\n*CHI:\thello world .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E542"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e543_header_out_of_order_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Options:\tCA\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|3;6|male|||Target_Child|||\n*CHI:\thello world .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E543"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e546_unsupported_ses_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|3;6|female||badses|Target_Child|||\n*CHI:\thello world .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E546"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e600_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Child\n@ID:\teng|corpus|CHI|||||Child|||\n*CHI:\thello world .\n%mor:\tint|hello n|world .\n%gra:\tabc|0|ROOT 2|0|ROOT\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E600"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e601_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Child\n@ID:\teng|corpus|CHI|||||Child|||\n*CHI:\thello .\nbadtier\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E326"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e602_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Child\n@ID:\teng|corpus|CHI|||||Child|||\n*CHI:\thello .\n%mor\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E602"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e603_invalid_tim_tier_format_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|||||Target_Child|||\n*CHI:\thello world .\n%tim:\tafternoon session\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E603"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e604_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Child\n@ID:\teng|corpus|CHI|||||Child|||\n*CHI:\thello world .\n%gra:\t1|2|SUBJ  2|0|ROOT\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E604"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e604_gra_without_mor_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|||||Target_Child|||\n*CHI:\thello world .\n%gra:\t1|2|SUBJ 2|0|ROOT\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E604"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e605_unsupported_dependent_tier_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|||||Target_Child|||\n*CHI:\thello world .\n%foo:\tunknown tier content\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E605"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e701_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|||||Target_Child|||\n*CHI:\thello . \u{15}5000_6000\u{15}\n*CHI:\tworld . \u{15}3000_4000\u{15}\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E701"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}

#[ignore = "Status: not_implemented (E702)"]
/// Tests expected behavior.
#[test]
fn test_e702_auto_begin_languages_eng_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let result = parser.parse_chat_file("@Begin\n@Languages:\teng\n@Participants:\tCHI Child\n@ID:\teng|corpus|CHI|||||Child|||\n*CHI:\thello world .\n%mor:\thello n|world .\n@End");

    let errors = match result {
        Ok(_) => return Err(talkbank_parser_tests::test_error::TestError::Failure("Expected parse error but parsing succeeded".to_string())),
        Err(errors) => errors,
    };

    let expected_codes = vec!["E702"];
    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e704_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|||||Target_Child|||\n*CHI:\thello ⌈ world ⌉ .\n*CHI:\t⌊ testing ⌋ .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E704"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e705_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|2;6|male|||Target_Child|||\n@Comment:\tNote: Tag markers are alignable content\n*CHI:\tI want ± cookie .\n%mor:\tpro|I v|want n|cookie .\n@Comment:\tERROR: Tag marker ± should have a mor item\n@Comment:\tMain tier alignable: I, want, ±, cookie = 4 words\n@Comment:\tMor tier: Should have 4 items (missing item for ±)\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E705"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e705_auto_utf8_begin_languages_1() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|2;6|male|||Target_Child|||\n*CHI:\tI want cookie .\n%mor:\tpro|I v|want .\n@Comment:\tERROR: Main tier has 3 words but %mor only has 2 items (missing n|cookie)\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E705"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e706_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|2;6|male|||Target_Child|||\n*CHI:\twant cookie .\n%mor:\tpro|I v|want n|cookie .\n@Comment:\tERROR: Main tier has 2 words but %mor has 3 items (extra pro|I)\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E706"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e706_auto_utf8_begin_languages_1() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|2;6|male|||Target_Child|||\n@Comment:\tNote: Retraces and scoped content don't align\n*CHI:\tI want [/] need cookie .\n%mor:\tpro|I v|want v|need n|cookie .\n@Comment:\tERROR: \"want\" is in a retrace [/] scope and shouldn't align\n@Comment:\tMain tier alignable: I, need, cookie = 3 words (want is excluded by [/])\n@Comment:\tMor tier: Should be pro|I v|need n|cookie (3 items + terminator)\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E706"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e706_auto_utf8_begin_languages_2() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|2;6|male|||Target_Child|||\n@Comment:\tNote: Events are non-alignable, like pauses\n*CHI:\tI &=laugh want cookie .\n%mor:\tpro|I v|laugh v|want n|cookie .\n@Comment:\tERROR: Event &=laugh shouldn't have a mor item (v|laugh is wrong)\n@Comment:\tMain tier alignable: I, want, cookie = 3 words (events excluded)\n@Comment:\tMor tier: Should be pro|I v|want n|cookie (3 items + terminator)\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E706"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e706_auto_utf8_begin_languages_3() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|2;6|male|||Target_Child|||\n@Comment:\tNote: Pauses, events, actions don't align - only words\n*CHI:\tI (.) want cookie .\n%mor:\tpro|I v|pause v|want n|cookie .\n@Comment:\tERROR: Pause (.) shouldn't have a mor item (v|pause is wrong)\n@Comment:\tMain tier alignable: I, want, cookie = 3 words (pauses excluded)\n@Comment:\tMor tier: Should be pro|I v|want n|cookie (3 items + terminator)\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E706"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e707_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|test|CHI||female|||Target_Child|||\n*CHI:\thello .\n%mor:\tco|hello\n@Comment:\tERROR: Main tier has terminator \".\" but %mor has none\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E707"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e707_utf8_begin_languages_1() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|test|CHI||female|||Target_Child|||\n*CHI:\thello\n%mor:\tco|hello .\n@Comment:\tERROR: %mor has terminator \".\" but main tier has none\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E707"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}

#[ignore = "Status: not_implemented (E708)"]
/// Tests expected behavior.
#[test]
fn test_e708_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Child\n@ID:\teng|corpus|CHI|||||Child|||\n*CHI:\thello world .\n%gra:\t|2|SUBJ 2|0|ROOT\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E708"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e709_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|||||Target_Child|||\n*CHI:\thello .\n%mor:\tco|hello .\n%gra:\t0|0|ROOT 1|0|PUNCT\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E709"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e710_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let result = parser.parse_chat_file("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Child\n@ID:\teng|corpus|CHI|||||Child|||\n*CHI:\thello world .\n%gra:\t1-2-SUBJ 2|0|ROOT\n@End");

    let errors = match result {
        Ok(_) => return Err(talkbank_parser_tests::test_error::TestError::Failure("Expected parse error but parsing succeeded".to_string())),
        Err(errors) => errors,
    };

    let expected_codes = vec!["E316"];
    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}

#[ignore = "Status: not_implemented (E711)"]
/// Tests expected behavior.
#[test]
fn test_e711_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|test|CHI||female|||Target_Child|||\n*CHI:\twant cookie .\n%mor:\tv| n|cookie .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E711"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}

#[ignore = "Status: not_implemented (E711)"]
/// Tests expected behavior.
#[test]
fn test_e711_utf8_begin_languages_1() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|test|CHI||female|||Target_Child|||\n*CHI:\twant cookie .\n%mor:\t|want n|cookie .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E711"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e712_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|||||Target_Child|||\n@Comment:\tERROR: Grammar relation word index out of bounds\n*CHI:\tI want .\n%mor:\tpro|I v|want .\n%gra:\t1|2|SUBJ 5|0|ROOT 3|2|PUNCT\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E712"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e713_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|||||Target_Child|||\n@Comment:\tERROR: head index 9 is out of range for 3 mor chunks\n*CHI:\tI want .\n%mor:\tpro|I v|want .\n%gra:\t1|2|SUBJ 2|0|ROOT 3|9|PUNCT\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E713"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e714_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|2;6|male|||Target_Child|||\n*CHI:\tI want cookie .\n%pho:\taɪ wɑnt\n@Comment:\tERROR: Main tier has 3 words but %pho only has 2 tokens (missing cookie)\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E714"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e715_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|2;6|male|||Target_Child|||\n*CHI:\twant cookie .\n%pho:\taɪ wɑnt kʊki\n@Comment:\tERROR: Main tier has 2 words but %pho has 3 tokens (extra aɪ)\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E715"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e716_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|test|CHI||female|||Target_Child|||\n*CHI:\thuh ?\n%mor:\tco|huh .\n@Comment:\tERROR: Main tier terminator is \"?\" but %mor terminator is \".\"\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E716"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e716_utf8_begin_languages_1() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|test|CHI||female|||Target_Child|||\n*CHI:\twow !\n%mor:\tco|wow .\n@Comment:\tERROR: Main tier terminator is \"!\" but %mor terminator is \".\"\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E716"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e716_utf8_begin_languages_2() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|test|CHI||female|||Target_Child|||\n*CHI:\tand then +...\n%mor:\tconj|and adv|then .\n@Comment:\tERROR: Main tier terminator is \"+...\" but %mor terminator is \".\"\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E716"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e718_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|2;6|male|||Target_Child|||\n*CHI:\tI want cookie .\n%sin:\tPOINT REACH\n@Comment:\tERROR: Main tier has 3 words but %sin only has 2 tokens (missing gesture for cookie)\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E718"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e719_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|2;6|male|||Target_Child|||\n*CHI:\twant cookie .\n%sin:\tPOINT REACH GRAB\n@Comment:\tERROR: Main tier has 2 words but %sin has 3 tokens (extra GRAB)\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E719"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e720_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|2;6|male|||Target_Child|||\n@Comment:\tNote: %gra aligns to %mor chunks, not items!\n*CHI:\tI want cookie .\n%mor:\tpro|I v|want n|cookie .\n%gra:\t1|2|SUBJ 2|0|ROOT 3|2|OBJ 4|4|EXTRA 5|2|PUNCT\n@Comment:\tERROR: %mor has 4 chunks (3 words + terminator) but %gra has 5 relations\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E720"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e721_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|||||Target_Child|||\n*CHI:\tI want cookie .\n%mor:\tpro|I v|want n|cookie .\n%gra:\t1|3|SUBJ 3|0|ROOT 2|3|OBJ 4|3|PUNCT\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E721"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e722_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|||||Target_Child|||\n*CHI:\tI want .\n%mor:\tpro|I v|want .\n%gra:\t1|2|SUBJ 2|1|OBJ\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E722"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e723_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|||||Target_Child|||\n*CHI:\tI want cookie .\n%mor:\tpro|I v|want n|cookie .\n%gra:\t1|1|ROOT 2|2|ROOT 3|1|OBJ 4|1|PUNCT\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E723"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e724_gra_circular_dependency_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|||||Target_Child|||\n*CHI:\thello there .\n%mor:\tco|hello adv|there .\n%gra:\t1|2|DISCOURSE 2|1|ADVMOD 3|1|PUNCT\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E724"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e724_gra_circular_dependency_utf8_begin_languages_1() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tPAR Participant\n@ID:\teng|corpus|PAR|||||Participant|||\n*PAR:\tbong , seven .\n%mor:\tn|bong punct|, num|seven .\n%gra:\t1|3|FLAT 2|3|PUNCT 3|1|APPOS 4|1|PUNCT\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E724"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e724_gra_circular_dependency_utf8_begin_languages_2() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|||||Target_Child|||\n*CHI:\tI want it .\n%mor:\tpro|I v|want pro|it .\n%gra:\t1|2|NSUBJ 2|3|ROOT 3|2|OBJ 4|2|PUNCT\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E724"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e725_modsyl_mod_count_mismatch_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\tnld\n@Participants:\tCHI Child, MOT Mother\n@ID:\tnld|corpus|CHI|2;0||||Child|||\n@ID:\tnld|corpus|MOT|||||Mother|||\n*MOT:\tmuts ja .\n%mod:\tˈmœts ˈja\n%xmodsyl:\tˈm:Oœ:Nt:Cs:R\n@Comment:\tERROR: %mod has 2 words but %xmodsyl has only 1\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E725"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e726_phosyl_pho_count_mismatch_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\tnld\n@Participants:\tCHI Child, MOT Mother\n@ID:\tnld|corpus|CHI|2;0||||Child|||\n@ID:\tnld|corpus|MOT|||||Mother|||\n*MOT:\tmuts ja .\n%pho:\tˈmʉst ˈjɛ\n%xphosyl:\tˈm:Oʉ:Ns:Ct:C\n@Comment:\tERROR: %pho has 2 words but %xphosyl has only 1\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E726"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e727_phoaln_mod_count_mismatch_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\tnld\n@Participants:\tCHI Child, MOT Mother\n@ID:\tnld|corpus|CHI|2;0||||Child|||\n@ID:\tnld|corpus|MOT|||||Mother|||\n*MOT:\tmuts ja .\n%mod:\tˈmœts ˈja\n%xphoaln:\tm↔m,œ↔ʉ,t↔s,s↔t\n@Comment:\tERROR: %mod has 2 words but %xphoaln has only 1\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E727"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e728_phoaln_pho_count_mismatch_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\tnld\n@Participants:\tCHI Child, MOT Mother\n@ID:\tnld|corpus|CHI|2;0||||Child|||\n@ID:\tnld|corpus|MOT|||||Mother|||\n*MOT:\tmuts ja .\n%pho:\tˈmʉst ˈjɛ\n%xphoaln:\tm↔m,œ↔ʉ,t↔s,s↔t\n@Comment:\tERROR: %pho has 2 words but %xphoaln has only 1\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E728"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e733_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|2;6|male|||Target_Child|||\n*CHI:\tI want cookie .\n%mod:\taɪ wɑnt\n@Comment:\tERROR: Main tier has 3 words but %mod only has 2 tokens (missing cookie)\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E733"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e734_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|2;6|male|||Target_Child|||\n*CHI:\twant cookie .\n%mod:\taɪ wɑnt kʊki\n@Comment:\tERROR: Main tier has 2 words but %mod has 3 tokens (extra aɪ)\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E734"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_e999_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Target_Child\n@ID:\teng|corpus|CHI|2;6|male|||Target_Child|||\n@Comment:\tNote: Terminators (. ! ?) align for %mor and %gra\n*CHI:\tI want cookie .\n%mor:\tpro|I v|want n|cookie\n@Comment:\tERROR: %mor missing terminator (should end with .)\n@Comment:\tMain tier: 3 words + terminator = 4 alignable\n@Comment:\tMor tier: Only 3 items (missing terminator)\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E707"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_w108_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Child\n@ID:\teng|corpus|CHI|||||Child|||\n*MOT:\thello .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["W108"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_w601_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Child\n@ID:\teng|corpus|CHI|||||Child|||\n@Comment:\tExpected tree-sitter warning: E342 (Empty dependent tier content)\n@Comment:\tExpected direct warning: W601\n*CHI:\thello world .\n%xfoo:\t\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["E342"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}

#[ignore = "Status: not_implemented (W602)"]
/// Tests expected behavior.
#[test]
fn test_w602_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Child\n@ID:\teng|corpus|CHI|||||Child|||\n*CHI:\thello world .\n%xpho:\thɛloʊ wɜɹld\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["W602"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}


/// Tests expected behavior.
#[test]
fn test_w999_auto_utf8_begin_languages_0() -> Result<(), talkbank_parser_tests::test_error::TestError> {
    let parser = TreeSitterParser::new()?;
    let sink = talkbank_model::ErrorCollector::new();
    let mut chat_file = parser.parse_chat_file_streaming("@UTF8\n@Begin\n@Languages:\teng\n@Participants:\tCHI Child\n@ID:\teng|corpus|CHI|||||Child|||\n@OldHeader:\tsome value\n*CHI:\thello .\n@End", &sink);

    // Run validation
    chat_file.validate_with_alignment(&sink, None);

    let errors = sink.into_vec();
    let expected_codes = vec!["W999"];

    for code in expected_codes {
        let expected = talkbank_model::ErrorCode::new(code);
        let has_expected = errors.iter().any(|err| err.code == expected);
        assert!(has_expected, "Expected error code {}, but got: {:?}",
            code, errors.iter().map(|err| err.code.as_str()).collect::<Vec<_>>());
    }

    Ok(())
}

