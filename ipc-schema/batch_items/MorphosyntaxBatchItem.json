{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "MorphosyntaxBatchItem",
  "description": "Batch item for morphosyntax NLP processing.",
  "type": "object",
  "properties": {
    "lang": {
      "description": "Language code for this utterance (ISO 639-3).",
      "type": "string"
    },
    "special_forms": {
      "description": "Special form and language per word: (form_type, resolved_language).",
      "type": "array",
      "items": {
        "type": "array",
        "maxItems": 2,
        "minItems": 2,
        "prefixItems": [
          {
            "type": [
              "string",
              "null"
            ]
          },
          {
            "type": [
              "string",
              "null"
            ]
          }
        ]
      }
    },
    "terminator": {
      "description": "Utterance terminator. Typed; serializes to its CHAT surface form\n(`.`, `?`, `!`, etc.) over the IPC boundary so the Stanza worker\ncontinues to receive a plain string.",
      "type": "string"
    },
    "words": {
      "description": "Word texts for NLP processing. Each word is provenance-sealed\n`ChatCleanedText` derived from a parsed `Word` or `Separator` —\nor, for non-`@s` special-form positions, the blessed\n`ChatCleanedText::stanza_placeholder()` constant.",
      "type": "array",
      "items": {
        "type": "string"
      }
    }
  },
  "required": [
    "words",
    "terminator",
    "special_forms",
    "lang"
  ]
}